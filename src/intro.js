"use strict";

var util    = require( "keigai" ).util,
    array   = util.array,
    iterate = util.iterate,
    request = util.request,
    btoa    = require( "btoa" ),
    HOST    = "https://fluidsurveys.com";
